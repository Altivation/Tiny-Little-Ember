using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerBlockTrigger : MonoBehaviour
{
    // Start is called before the first frame update
    Rigidbody2D parentrb;
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
